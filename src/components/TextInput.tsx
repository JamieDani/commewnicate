import { Stack, TextField } from "@mui/material"

export const TextInput = () => {
    return (
        <Stack spacing={4}>
            <TextField label='Write a description'/>
        </Stack>
    )
}